import 'package:flutter/material.dart';

class Traits extends StatelessWidget {
  const Traits({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Text('Traits Screen'));
  }
}
